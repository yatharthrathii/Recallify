export * from './common';

/**
 * Phase 4 adds: auth.ts, deck.ts, card.ts, review.ts, ai.ts, stats.ts.
 * Contract first, then endpoint, then UI -- see docs/05-ENGINEERING.md,
 * "Definition of done".
 */
